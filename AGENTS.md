# AGENTS.md

## Commands

- Use `pnpm` only. The lockfile is `pnpm-lock.yaml`.
- Dev server: `pnpm dev`
- Production build: `pnpm build`
- Production server: `pnpm start`
- Lint: `pnpm lint`
- Format whole repo: `pnpm format`
- Convex: `pnpm convex:dev`, `pnpm convex:push`, `pnpm convex:deploy`

## Verification

- There is no test suite and no `typecheck` script.
- The meaningful verification path is `pnpm build && pnpm lint`.
- Pre-commit only runs `npx lint-staged`, which formats staged files with Prettier.
- `src/routeTree.gen.ts` is generated by TanStack Router during route builds/dev and is ignored by Prettier. Expect it to change when editing routes.
- run `npx react-doctor@latest` and secure everything is fine

## Architecture

- This is a single-package TanStack Start app, not a monorepo.
- Runtime entrypoints are `src/client.tsx`, `src/router.tsx`, and `src/routes/__root.tsx`.
- Canonical app routes are `/projects`, `/issues`, `/issues/$issueIdentifier`, and `/settings`.
- `src/routes/$orgId*` files are legacy compatibility redirects only.
- Shared app shell lives in `components/layout/main-layout.tsx` and `components/layout/sidebar/*`.
- Convex schema and functions live in `convex/*`.
- Client data modules live in `src/data/*` and should use Convex React/TanStack Query integration directly.
- `lib/db/*` is a compatibility type surface for existing UI imports.
- Client state under `store/*` should stay UI-oriented. Do not reintroduce a Zustand cache for Convex-backed remote data.

## Product Scope

- Treat the product as a personal issue tracker fork, not a team/workspace app.
- Do not reintroduce org, workspace, or team abstractions unless explicitly requested.
- `inbox` and much of `settings` are low-priority placeholder surfaces; avoid polishing them unless the task requires it.
- The current user model is intentionally single-user and lives in `lib/current-user.ts`.

## Data Reality

- Projects, issues, labels, project statuses, priorities, project updates, viewer profile, and viewer preferences are backed by Convex.
- Avoid reintroducing `mock-data/*` or seed-driven sample content as product dependencies.
- `VITE_CONVEX_URL` is required for the browser Convex client. See `.env.example`.
- The Convex CLI targets the same self-hosted deployment used by production and development through `CONVEX_SELF_HOSTED_URL` and `CONVEX_SELF_HOSTED_ADMIN_KEY`.
- Do not create or select local/anonymous Convex deployments for normal development.

## Styling And UI

- Preserve the existing Circle visual language; do not invent a new design direction unless asked.
- If a refactor weakens the UI, use `/home/vrivera/git-packages/circle` as the visual reference.
- Shadcn aliases are rooted at `@/`, but `components.json` still points at the old CSS path. The real global stylesheet is `src/styles/globals.css`.

## Ownership

- Keep `LICENSE.md` intact.
- Do not reintroduce original template branding or external promotional links unless explicitly requested.
